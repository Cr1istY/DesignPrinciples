### 依赖倒置原则（DIP）

在设计代码架构时，高层模块不应该依赖于底层模块，二者都应该依赖于抽象。

抽象不应该依赖于细节，细节应该依赖于抽象。

### 解决的问题

当违法依赖倒置原则时，会出现以下问题：

1. 如果程序是一次性的、几乎不变的那么可以不考虑很多的扩展性和可维护性因素。
2. 但如果这些程序具有不确定性或者当业务发展时需要不断地调整和新增，那么这样的实现方式就很不友好了，首先，每次扩展都要新增接口，长久下来，一个类会变得代码冗长，难以维护。

